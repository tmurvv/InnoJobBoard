work on look of message box
when category updated, all job listings updated
validations sanitizing on all edits and adds
Create Contact Page
figure out linkIn jpg error correctly
Make it so admin page does not open new window
Make a warning sign that editing a selector changes all selectors. and remove orange box.